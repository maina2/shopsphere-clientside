export interface Product {
    id: number;
    name: string;
    description: string;
    price: number;
    stock: number;
    category: number;
    image:string
    // Add other fields if needed, such as image, created_at, etc.
  }
  